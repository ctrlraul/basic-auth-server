This repository is merely intended as a reference for myself.

#### **Installation**

Clone repository and run `npm i`

#### Running

- For testing: `npm run dev`
- Build and run: `npm run build` and then `npm start`
